# 独立桌宠运行端 PRD

版本: v0.1  
日期: 2026-06-24  
阶段: 第一版产品定义  
目标平台: macOS、Windows  
技术倾向: Electron 桌面应用  

## 1. 产品概述

本产品是一个脱离 Codex 独立运行的桌面宠物应用。第一版聚焦“桌面运行端”, 不内置宠物生成能力, 但直接兼容 Codex Hatch Pet 生成的宠物包, 用户可以导入已有的 `pet.json + spritesheet.webp` 包并让宠物在 macOS 和 Windows 桌面上常驻、互动、可隐藏、可拖拽、可配置。

第一版的核心价值不是生成新宠物, 而是先把“宠物包运行协议”和“跨平台桌面体验”打稳。后续再接入“上传一张照片 -> 生成角色和动作 -> 导入运行”的生产链路。

一句话定位:

> 一个可以运行 Hatch Pet 宠物包的跨平台独立桌宠播放器, 后续扩展为用户自定义照片桌宠平台。

## 2. 背景与依据

市场调研显示, 桌宠产品的成熟能力集中在透明桌面浮层、鼠标追踪、拖拽、点击反馈、窗口/屏幕边界互动、托盘控制和模组/资产生态。Codex Hatch Pet 已经提供了稳定的宠物资产生产规范: 固定 atlas、透明背景、状态行、`pet.json`、`spritesheet.webp`、contact sheet 和 QA 校验。第一版独立应用应优先复用该规范, 避免同时攻克“运行时”和“生成端”两个复杂问题。

产品边界:

- 做: macOS/Windows 桌面运行端、Hatch Pet 宠物包导入和播放、基础桌面互动、可扩展动作协议。
- 不做: 照片上传生成、图像生成服务、宠物资产编辑器、社区市场、账号系统、云同步。

## 3. 目标

### 3.1 产品目标

- 用户可以导入 Hatch Pet 生成的宠物包, 并在桌面上看到宠物正常播放。
- 桌宠默认不打扰用户工作, 但能通过鼠标、点击、拖拽产生基础互动。
- 同一套宠物包能在 macOS 和 Windows 上运行。
- 资产协议保持向后兼容 Hatch Pet v1, 同时为更多动作和交互预留扩展字段。
- 第一版形成可验证的运行端基础, 便于后续接入照片生成宠物包。

### 3.2 成功标准

- 导入一个合法 Hatch Pet 包后, 5 秒内宠物出现在桌面。
- `idle`、`running-right`、`running-left` 等基础状态播放正确, 不出现白底、错位或明显帧跳。
- 用户可以拖拽宠物、缩放宠物、隐藏/召回宠物、退出应用。
- 透明区域不阻挡用户点击桌面或其他应用。
- macOS 和 Windows 都可以安装、启动、导入、运行同一个宠物包。
- 应用空闲时 CPU 占用保持低位, 不明显影响日常办公。

## 4. 用户与场景

### 4.1 目标用户

- 已经拥有 Hatch Pet 宠物包, 想脱离 Codex 独立运行的人。
- 喜欢桌面陪伴、桌宠、虚拟宠物的用户。
- 后续会上传真实宠物照片生成桌宠的普通用户。
- 需要测试宠物包效果的创作者或开发者。

### 4.2 核心场景

1. 用户拿到一个 Hatch Pet 包, 直接拖入应用, 桌面出现宠物。
2. 用户工作时宠物在角落待机、走动或轻微看向鼠标, 不遮挡主要操作。
3. 用户点击或摸宠物, 宠物触发问候、跳跃、等待等反馈动作。
4. 用户需要专注时从托盘一键隐藏宠物, 之后再召回。
5. 用户导入多个宠物包, 在宠物库里切换当前桌宠。

## 5. 第一版范围

### 5.1 P0 必须实现

- Electron 桌面应用壳。
- macOS 与 Windows 安装包。
- 透明无边框桌宠窗口。
- Hatch Pet v1 宠物包导入。
- Hatch Pet 固定 atlas 播放。
- 基础状态机: `idle`、`running-right`、`running-left`、`waving`、`jumping`、`failed`、`waiting`、`running`、`review`。
- 拖拽宠物。
- 宠物缩放。
- 点击宠物触发动作。
- 基础点击穿透: 宠物可见区域接收事件, 透明区域尽量不阻挡桌面操作。
- 托盘/菜单栏菜单: 显示/隐藏、切换宠物、设置、退出。
- 本地宠物库。
- 合法性校验和错误提示。

### 5.2 P1 建议实现

- 像素级点击穿透。
- 宠物 alpha mask 命中检测。
- 多显示器支持。
- 开机自启动。
- 安静模式。
- 降低帧率/省电模式。
- 宠物位置和大小记忆。
- 导入 `.zip` 宠物包。
- 最近导入记录。

### 5.3 P2 后续扩展

- 多宠物同屏。
- 自定义动作映射。
- `sleep`、`eat`、`petting`、`play` 等宠物化动作。
- 窗口边缘互动: 坐在窗口上、贴边探头。
- 轻养成: 心情、能量、亲密度。
- 照片生成宠物包服务。
- 宠物包分享和市场。

## 6. 非目标

第一版明确不做:

- 不做上传照片生成宠物。
- 不做图像生成和动作生成。
- 不做宠物包编辑器。
- 不做在线账号、登录、支付。
- 不做社区分享和审核。
- 不做复杂 3D/VRM/MMD 模型导入。
- 不做强干扰行为, 例如抢鼠标、强制弹窗、遮挡用户输入。

## 7. 关键产品流程

### 7.1 首次启动

1. 用户打开应用。
2. 应用显示一个轻量欢迎窗口。
3. 用户选择“导入宠物包”或“打开示例宠物”。
4. 导入成功后, 欢迎窗口收起, 桌面出现宠物。
5. 托盘图标常驻, 提供控制入口。

### 7.2 导入 Hatch Pet 包

支持三种入口:

- 选择包含 `pet.json` 和 `spritesheet.webp` 的文件夹。
- 选择 `.zip` 包, 应用解压后校验。
- 选择 Codex 默认宠物目录下的某个包: `${CODEX_HOME:-$HOME/.codex}/pets/<pet-name>/`。

导入步骤:

1. 读取 `pet.json`。
2. 定位 `spritesheetPath`。
3. 校验图片格式、尺寸、透明通道和基础 metadata。
4. 复制到应用自己的本地宠物库。
5. 写入本地索引。
6. 加载并播放 `idle`。

导入失败时, 提示必须明确说明原因:

- 缺少 `pet.json`。
- 缺少 `spritesheet.webp`。
- `spritesheetPath` 指向不存在。
- atlas 尺寸不是 `1536x1872`。
- 图片无法读取。
- manifest JSON 无法解析。

### 7.3 桌面运行

宠物出现后默认进入 `idle`。应用根据事件切换状态:

- 鼠标靠近: 宠物轻微看向鼠标, 保持 `idle` 或短暂进入 `waiting`。
- 点击宠物: 播放 `waving` 或 `jumping`。
- 拖拽宠物: 根据拖拽方向播放 `running-right` 或 `running-left`。
- 拖拽结束: 回到 `idle`。
- 应用内部忙碌或未来任务状态: 播放 `running`。
- 错误或加载失败: 播放 `failed`。
- 用户打开设置或等待确认: 可播放 `waiting`。
- 用户打开宠物详情或检查状态: 可播放 `review`。

第一版不要求宠物自动走满桌面, 但需要支持轻微随机位移或停留动作, 以避免完全静态。

### 7.4 控制和设置

托盘/菜单栏菜单必须包含:

- 显示/隐藏宠物。
- 当前宠物名称。
- 切换宠物。
- 导入宠物包。
- 设置。
- 退出。

设置项 P0:

- 宠物大小: 50%-200%。
- 置顶: 开/关。
- 安静模式: 开/关。
- 启动时显示上次宠物: 开/关。
- 当前宠物位置重置。

设置项 P1:

- 开机启动。
- 帧率: 低/标准/高。
- 鼠标穿透模式。
- 全屏应用时自动隐藏。
- 多显示器显示位置。

## 8. Hatch Pet v1 兼容协议

### 8.1 文件结构

第一版必须支持 Hatch Pet 当前包结构:

```text
<pet-name>/
  pet.json
  spritesheet.webp
```

也允许未来包内包含更多文件:

```text
<pet-name>/
  pet.json
  spritesheet.webp
  preview.png
  metadata.json
  sounds/
  actions/
```

第一版遇到未知文件时应忽略, 不应导入失败。

### 8.2 Hatch Pet v1 manifest

当前 Hatch Pet manifest 形态:

```json
{
  "id": "pet-name",
  "displayName": "Pet Name",
  "description": "One short sentence.",
  "spritesheetPath": "spritesheet.webp"
}
```

第一版必须支持这个最小 manifest。若缺少 `displayName` 或 `description`, 应使用 `id` 和默认文案兜底。若缺少 `id` 或 `spritesheetPath`, 导入失败。

### 8.3 固定 atlas 规格

Hatch Pet v1 固定使用:

| 字段 | 规格 |
| --- | --- |
| 图片格式 | PNG 或 WebP, 第一版优先支持 WebP |
| atlas 尺寸 | `1536x1872` |
| 网格 | 8 列 x 9 行 |
| 单格尺寸 | `192x208` |
| 背景 | 透明 |
| 空格 | 未使用 cell 必须透明 |

### 8.4 默认状态行

第一版在 manifest 没有 `animations` 字段时, 按以下约定播放:

| Row | State | Used columns | Duration |
| --- | --- | ---: | --- |
| 0 | `idle` | 0-5 | 280, 110, 110, 140, 140, 320 ms |
| 1 | `running-right` | 0-7 | 120 ms each, final 220 ms |
| 2 | `running-left` | 0-7 | 120 ms each, final 220 ms |
| 3 | `waving` | 0-3 | 140 ms each, final 280 ms |
| 4 | `jumping` | 0-4 | 140 ms each, final 280 ms |
| 5 | `failed` | 0-7 | 140 ms each, final 240 ms |
| 6 | `waiting` | 0-5 | 150 ms each, final 260 ms |
| 7 | `running` | 0-5 | 120 ms each, final 220 ms |
| 8 | `review` | 0-5 | 150 ms each, final 280 ms |

### 8.5 扩展 manifest 草案

为未来更多动作保留扩展字段。第一版可以识别但不强依赖:

```json
{
  "id": "momo",
  "displayName": "Momo",
  "description": "A custom desktop pet.",
  "spritesheetPath": "spritesheet.webp",
  "schemaVersion": "1.1",
  "cell": {
    "width": 192,
    "height": 208
  },
  "atlas": {
    "columns": 8,
    "rows": 9
  },
  "animations": {
    "idle": {
      "row": 0,
      "frames": [0, 1, 2, 3, 4, 5],
      "durationsMs": [280, 110, 110, 140, 140, 320],
      "loop": true
    },
    "petting": {
      "row": 9,
      "frames": [0, 1, 2, 3, 4, 5],
      "durationsMs": [120, 120, 120, 120, 120, 240],
      "loop": false,
      "fallback": "waving"
    }
  },
  "interactions": {
    "click": "waving",
    "doubleClick": "jumping",
    "dragLeft": "running-left",
    "dragRight": "running-right",
    "idle": "idle"
  }
}
```

兼容规则:

- 没有 `schemaVersion` 时按 Hatch Pet v1 处理。
- 没有 `animations` 时使用固定行约定。
- 有 `animations` 时优先使用 manifest 显式配置。
- 遇到运行端不认识的 action, 使用 `fallback`。
- 未配置 `fallback` 时回退到 `idle`。
- 不允许因为出现未知扩展字段而导入失败。

## 9. 桌面窗口需求

### 9.1 窗口形态

- 透明背景。
- 无边框。
- 默认置顶。
- 不显示在任务栏或 Dock 常规窗口列表中, 主要通过托盘/菜单栏控制。
- 可拖拽移动。
- 可跨屏幕移动。
- 记录上次位置。

### 9.2 点击命中与穿透

桌宠不能用透明矩形挡住用户桌面。第一版推荐实现两级命中:

1. P0: 使用宠物当前帧的非透明包围盒作为命中区域。
2. P1: 使用当前帧 alpha mask 做更精准的像素级命中。

行为规则:

- 鼠标在宠物可见区域内: 接收点击、拖拽、悬停。
- 鼠标在透明区域内: 事件穿透到底下窗口。
- 拖拽中: 暂时关闭穿透, 保证拖拽稳定。
- 打开右键菜单时: 暂时关闭穿透。

### 9.3 多显示器

第一版要求:

- 能识别当前显示器边界。
- 宠物不能被拖到完全不可见区域。
- 重启后如果原显示器不存在, 自动回到主屏幕可见区域。

P1:

- 支持用户选择默认显示器。
- 支持全屏应用时隐藏。

## 10. 渲染与状态机

### 10.1 渲染器

推荐使用 Canvas 2D 或 PixiJS 播放 atlas。第一版优先 Canvas 2D, 减少依赖。

渲染要求:

- 支持 WebP 透明图。
- 支持按 row/column 裁切绘制。
- 支持逐帧 duration。
- 支持缩放。
- 支持水平翻转, 但 Hatch Pet v1 已提供 `running-left`, 默认不需要运行时镜像。
- 支持 reduced motion: 使用 `idle` 第一帧作为静态宠物。

### 10.2 状态机

状态机分三层:

- Base state: 当前长期状态, 如 `idle`、`running`。
- Interaction state: 点击、拖拽等短动作, 如 `waving`、`jumping`。
- System state: 加载、错误、隐藏、暂停。

优先级:

1. System state。
2. Drag state。
3. Click/gesture action。
4. Scheduled idle variation。
5. Default idle。

状态切换规则:

- 短动作播放完成后回到上一个 base state。
- 拖拽过程中根据方向切换左右移动动作。
- 无输入超过指定时间回到 `idle`。
- 加载失败时进入 `failed`, 并展示错误说明。

## 11. 宠物库

### 11.1 本地存储

应用维护独立宠物库, 不直接依赖 Codex 目录:

```text
app-data/
  pets/
    <pet-id>/
      pet.json
      spritesheet.webp
  library.json
  settings.json
```

导入 Codex 宠物包时, 复制一份到应用宠物库。这样即使 Codex 目录变化, 独立应用仍可运行。

### 11.2 宠物库能力

P0:

- 查看已导入宠物。
- 切换当前宠物。
- 删除宠物。
- 重新导入覆盖。

P1:

- 显示宠物预览。
- 显示来源路径。
- 校验宠物包健康状态。
- 导出宠物包。

## 12. 设置与安全

### 12.1 权限原则

第一版不需要屏幕录制权限, 不需要辅助功能权限, 不读取用户窗口内容。桌宠只需要:

- 文件选择权限。
- 本地文件读写权限。
- 开机启动权限, 仅用户开启时使用。

### 12.2 隐私原则

- 不上传宠物包。
- 不上传用户路径或文件名。
- 不内置遥测作为 P0 必需项。
- 若后续加入崩溃日志或匿名统计, 必须默认透明说明并允许关闭。

### 12.3 安全原则

- 导入 zip 时防止路径穿越。
- 不执行宠物包内任何脚本。
- `pet.json` 只作为数据读取。
- 忽略未知可执行文件。
- 限制单个宠物包大小, 避免异常资源耗尽。

## 13. 跨平台要求

### 13.1 macOS

- 支持 Apple Silicon 与 Intel, 或至少明确首发架构。
- 提供 `.dmg` 安装包。
- 支持菜单栏控制。
- 支持透明窗口和置顶。
- 支持多显示器。
- 支持应用签名和公证作为发布前要求。

### 13.2 Windows

- 支持 Windows 10/11。
- 提供 `.exe` 或 `.msi` 安装包。
- 支持系统托盘。
- 支持透明窗口和置顶。
- 支持多显示器。
- 支持开机启动。

### 13.3 技术选型建议

首版建议 Electron:

- 透明浮窗、托盘、跨平台打包成熟。
- 渲染层可以直接使用 Canvas/PixiJS。
- 后续接入本地文件、zip 导入、配置管理较快。
- 代价是包体和内存更大, 但桌宠第一版可通过低帧率和暂停策略控制资源。

## 14. 性能要求

P0 目标:

- 空闲状态 CPU 占用保持低位。
- 默认帧率不超过 30 FPS。
- 宠物隐藏时暂停渲染。
- 屏幕锁定或系统休眠恢复后正常恢复。
- 单宠物内存占用可接受, 不因切换宠物持续增长。

P1:

- 安静模式下降到 10-15 FPS。
- 长时间无交互时降频。
- 全屏游戏或视频时自动隐藏或暂停。

## 15. 错误和空状态

### 15.1 无宠物状态

展示轻量窗口, 提供:

- 导入宠物包。
- 打开示例宠物。
- 查看支持格式。

### 15.2 导入失败

错误提示应给出:

- 失败原因。
- 当前检测到的文件。
- 用户可以怎么修复。

示例:

```text
无法导入这个宠物包: atlas 尺寸应为 1536x1872, 当前为 1024x1024。
请确认这是由 Hatch Pet 生成的完整宠物包。
```

### 15.3 运行失败

- 宠物图片加载失败: 回到宠物库并标记异常。
- 当前宠物被删除: 自动切换到下一个可用宠物或无宠物状态。
- 设置损坏: 使用默认设置启动, 并备份损坏文件。

## 16. 验收标准

### 16.1 宠物包兼容

- 可以导入 Hatch Pet v1 文件夹。
- 可以导入 Hatch Pet v1 zip 包。
- 可以读取最小 `pet.json`。
- 可以按固定 row/duration 播放 atlas。
- 未使用 cell 不影响播放。
- 未知扩展字段不影响导入。

### 16.2 桌面体验

- 宠物能显示在 macOS 桌面。
- 宠物能显示在 Windows 桌面。
- 透明背景正常。
- 可拖拽。
- 可缩放。
- 可隐藏/召回。
- 可退出。
- 不阻塞透明区域下方窗口点击。

### 16.3 交互体验

- 点击宠物触发短动作。
- 拖拽方向触发左右移动状态。
- 短动作结束回到 `idle`。
- 错误状态可展示并恢复。

### 16.4 稳定性

- 连续运行 4 小时不崩溃。
- 切换宠物 20 次不出现明显内存泄漏。
- 多显示器插拔后宠物仍可找回。
- 应用异常退出后重启可恢复上次宠物和位置。

## 17. 里程碑

### M0: 技术验证

- Electron 透明无边框窗口。
- Canvas 播放固定 atlas。
- 加载一个本地 Hatch Pet 包。
- 拖拽和缩放。

### M1: 可用 MVP

- 宠物包导入。
- 本地宠物库。
- 托盘控制。
- 基础状态机。
- 基础点击穿透。
- macOS/Windows 双平台打包。
- 导入校验和错误提示。

### M2: 体验完善

- 像素级点击穿透。
- 多显示器稳定。
- 安静模式。
- 开机启动。
- zip 导入。
- 宠物预览和详情。

### M3: 生成端接入准备

- 扩展 manifest。
- 更多宠物动作协议。
- 生成服务导出的宠物包直接导入。
- 动作兼容和 fallback 机制。

## 18. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| macOS/Windows 透明窗口行为不一致 | 宠物显示或点击穿透异常 | 先做最小桌面窗口 PoC, 将平台差异封装在 window adapter |
| Hatch Pet 包只有最小 manifest | 缺少显式动画配置 | 第一版内置 Hatch Pet v1 固定 row/duration 约定 |
| 透明区域挡住用户点击 | 影响日常使用 | P0 做包围盒穿透, P1 做 alpha mask 命中 |
| Electron 资源占用偏高 | 用户长期运行不舒服 | 默认低帧率、隐藏暂停、空闲降频 |
| 宠物包格式未来变化 | 兼容成本增加 | 引入 schemaVersion 和 fallback, 未知字段忽略 |
| 用户导入异常 zip | 安全风险 | 禁止执行脚本、防路径穿越、限制包大小 |

## 19. 待确认问题

- 第一版是否内置一个示例宠物包。
- 首发是否要求 Mac App Store, 还是先用 DMG 分发。
- Windows 首发使用 `.exe` 还是 `.msi`。
- 像素级点击穿透是否提前进入 P0, 还是先用包围盒命中完成基础体验。
- 是否需要读取 Codex 默认宠物目录并一键导入。
- 第一版是否支持多个宠物同时运行。

## 20. 推荐开发拆分

1. 建立 Electron 应用骨架。
2. 实现 PetWindow: 透明、无边框、置顶、位置管理。
3. 实现 PetRenderer: atlas 加载、行列裁切、duration 播放。
4. 实现 HatchPetLoader: 读取 `pet.json`、校验 `spritesheet.webp`。
5. 实现 PetLibrary: 导入、复制、索引、切换、删除。
6. 实现 BehaviorEngine: 状态机和事件映射。
7. 实现 TrayController: 显示/隐藏、导入、设置、退出。
8. 实现 Settings: 大小、置顶、安静模式、位置重置。
9. 做 macOS/Windows 打包和冒烟测试。
10. 加入扩展 manifest 支持和 fallback 机制。
